#!/usr/bin/python3
''' module checking if obj is instance for same class '''


def is_same_class(obj, a_class):
    ''' method checks if obj is instance for same class
        Return : true if obj is instnce otherwise false.
    '''

    return (isinstance(obj, a_class))
